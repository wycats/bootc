{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SystemdServicesManifest",
  "description": "Declares the intended runtime state of systemd services. This is Tier 2 state — it lives in /etc/systemd/system/ and can drift from the declared manifest.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": ["string", "null"]
    },
    "services": {
      "description": "Map of unit names to their intended state",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/ServiceState"
      }
    }
  },
  "required": ["services"],
  "$defs": {
    "ServiceState": {
      "description": "The intended state of a systemd service",
      "type": "string",
      "enum": ["enabled", "disabled", "masked"]
    }
  }
}
