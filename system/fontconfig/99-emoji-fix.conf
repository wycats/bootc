<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<!--
  Fix emoji rendering in Electron/Chromium apps.
  
  Twemoji (3.2MB) is preferred over Noto Color Emoji (10.6MB) because:
  1. Smaller CBDT tables work better with Chromium's Skia renderer
  2. Noto Color Emoji v2.047+ has oversized CBDT tables that can cause
     rendering issues in some Chromium versions
  
  This config overrides 60-generic.conf which sets Noto Color Emoji first.
  We use <match> rules to prepend Twemoji to the family list.
-->
<fontconfig>
  <!-- Override the emoji family preference from 60-generic.conf -->
  <!-- Prepend Twemoji to any request for the generic emoji family -->
  <match target="pattern">
    <test name="family" compare="eq">
      <string>emoji</string>
    </test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Twemoji</string>
    </edit>
  </match>

  <!-- Also prepend Twemoji when Noto Color Emoji is explicitly requested -->
  <!-- (Even though we remove the package, some apps might still request it by name) -->
  <match target="pattern">
    <test name="family" compare="eq">
      <string>Noto Color Emoji</string>
    </test>
    <edit name="family" mode="prepend" binding="strong">
      <string>Twemoji</string>
    </edit>
  </match>

  <!-- Cross-platform emoji font aliases -->
  <alias binding="strong">
    <family>Apple Color Emoji</family>
    <prefer><family>Twemoji</family></prefer>
  </alias>

  <alias binding="strong">
    <family>Segoe UI Emoji</family>
    <prefer><family>Twemoji</family></prefer>
  </alias>

  <alias binding="strong">
    <family>Segoe UI Symbol</family>
    <prefer>
      <family>Twemoji</family>
      <family>Symbola</family>
    </prefer>
  </alias>

  <!-- Add emoji and symbol fonts as fallback for sans-serif -->
  <match target="pattern">
    <test name="family"><string>sans-serif</string></test>
    <edit name="family" mode="append" binding="weak">
      <string>Twemoji</string>
      <string>Symbola</string>
    </edit>
  </match>

  <!-- Add emoji and symbol fonts as fallback for serif -->
  <match target="pattern">
    <test name="family"><string>serif</string></test>
    <edit name="family" mode="append" binding="weak">
      <string>Twemoji</string>
      <string>Symbola</string>
    </edit>
  </match>

  <!-- Add emoji and symbol fonts as fallback for monospace -->
  <match target="pattern">
    <test name="family"><string>monospace</string></test>
    <edit name="family" mode="append" binding="weak">
      <string>Twemoji</string>
      <string>Symbola</string>
    </edit>
  </match>

  <!-- JetBrains Nerd Font with emoji fallback -->
  <alias>
    <family>JetBrainsMono Nerd Font</family>
    <prefer>
      <family>JetBrainsMono Nerd Font</family>
      <family>Twemoji</family>
      <family>Symbola</family>
    </prefer>
  </alias>
</fontconfig>
